document.addEventListener("DOMContentLoaded", function() {
    var options = {}; // Opções de configuração, se necessário
    var scrollbar = SmoothScrollbar.init(document.querySelector("#my-scrollbar"), options);
  });